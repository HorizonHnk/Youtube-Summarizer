// UI Components Index
// This file prevents the "empty chunk" warning during build

// Export any UI components here when you create them
// Example:
// export { default as Button } from './Button'
// export { default as Modal } from './Modal'

// For now, export an empty object to prevent build warnings
export default {}